import {Card, CardContent, CardHeader, CardTitle} from "./ui/card";

export const Radar = () => {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Radar</CardTitle>
            </CardHeader>
            <CardContent>
                <h1>Radar</h1>
            </CardContent>
        </Card>
    );
};